# Production build (vite build / Vercel) için API adresi.
# Bu değer .env içindeki localhost'u production modunda ezer.
# Sadece public backend URL'i içerir; gizli bilgi yoktur.
VITE_API_URL=https://web-e-ticaret.onrender.com/api
